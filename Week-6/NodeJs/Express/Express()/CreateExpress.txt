Install Express through CLI:

$ npm install express --save

Creates an Express application. 
The express() function is a top-level function exported by the express module.

var express = require('express');
var app = express();